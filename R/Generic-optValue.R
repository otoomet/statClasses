### optValue: optimal ojective function value of the estimated model

setGeneric("optValue",
           function(object, ...) {
              res <- standardGeneric("optValue")
              res
           }
           )
